# Live Chart

# Developing

In SBT:

    ~fastLinkJS

In another terminal:

    npm run dev

# Building

In SBT:

    ~fullLinkJS

In another terminal:

    npm run build      <-- this is what places artifacts in `dist`
    npm run preview
